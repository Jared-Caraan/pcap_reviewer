## File handling mode

We've already worked with the `open` function that tries to access a specific file and create a handle for it. However, we haven't seen all the options it offers.

### `open`

<img width="1097" height="842" alt="image" src="https://github.com/user-attachments/assets/d302964f-04e1-4a95-8ee6-2ae88d8ca111" /><br>

The `open` function that creates a stream for us can take many arguments. The most important ones are the first and the second argument.
